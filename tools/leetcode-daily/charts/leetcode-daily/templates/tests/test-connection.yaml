apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "leetcode-daily.fullname" . }}-test-connection"
  labels:
    {{- include "leetcode-daily.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "leetcode-daily.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
